import fetch from 'node-fetch';

describe('GET /', () => {
    beforeAll(async () => {
        
    });
    it('should be STATUS 200', () => {
        
    });
    it('should say Hello World!', () => {
        
    });
});